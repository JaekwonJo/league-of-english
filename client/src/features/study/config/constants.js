import problemTypes from '../../../config/problemTypes.json';

export const MAX_TOTAL_PROBLEMS = 20;
export const PROBLEM_STEP = 1;
export const PROBLEM_TYPES = problemTypes.problemTypes || {};
export const TYPE_KEYS = Object.keys(PROBLEM_TYPES);
